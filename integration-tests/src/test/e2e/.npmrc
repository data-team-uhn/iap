# Replace a node_modules built by a different pnpm without asking. Maven never gives the install a
# TTY, so the interactive confirmation pnpm would otherwise wait for aborts the build outright
# (ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY) whenever the directory was left by another version.
confirm-modules-purge=false

{
  "jcr:primaryType": "wf:WorkflowDefinition",
  "title": "Create a system workflow",
  "v1": {
    "jcr:primaryType": "wf:WorkflowVersion",
    "version": "1.0",
    "state": "ACTIVE",
    "targetResourceType": "wf/SystemWorkflowsHomepage",
    "description": "What happens when a workflow is asked for under /SystemWorkflows: the same thing as under /Workflows - an empty definition, ready for the editor - which is the point, since only its location makes a definition a system workflow. A definition of its own rather than a second target on createWorkflow because the two are different acts to govern: one adds a process a deployment runs, the other adds behavior the platform performs on its own, and a deployment that wants those held to different people changes this start event's performers without touching the other. A version's targetResourceType names one type, so covering both homepages from one definition would also have meant naming a supertype that every entity homepage shares - which would have had this catch creation on /Submissions too.",
    "requested": {
      "jcr:primaryType": "wf:StartEvent",
      "elementId": "requested",
      "label": "System workflow requested",
      "messageName": "create",
      "performers": ["iap-administrators"],
      "jcr:reference:flowNodeType": "/WorkflowTypes/MessageStartEvent",
      "toCreate": {
        "jcr:primaryType": "wf:SequenceFlow",
        "elementId": "toCreate",
        "targetRef": "create"
      }
    },
    "create": {
      "jcr:primaryType": "wf:Activity",
      "elementId": "create",
      "label": "Create the system workflow",
      "handler": "createEntity",
      "entityType": "wf:WorkflowDefinition",
      "jcr:reference:flowNodeType": "/WorkflowTypes/ServiceTask",
      "toDone": {
        "jcr:primaryType": "wf:SequenceFlow",
        "elementId": "toDone",
        "targetRef": "done"
      }
    },
    "done": {
      "jcr:primaryType": "wf:EndEvent",
      "elementId": "done",
      "label": "System workflow created",
      "jcr:reference:flowNodeType": "/WorkflowTypes/EndEvent"
    }
  }
}

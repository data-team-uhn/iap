{
  "jcr:primaryType": "ext:Extension",
  "ext:pointId": "iap/coreUI/view",
  "ext:name": "Version",
  "ext:hint": "One version's diagram, read-only: no palette, no context pad, nothing that could write. A .edit suffix opens the same page in editing mode - the same view and the same crumb, since editing a version is not a page below looking at it. The suffix rides on the last path segment, which this view's :version parameter matches along with the version's own name; the page takes it off again.",
  "ext:targetURL": "/admin/workflows/:homepage/:workflow/:version",
  "ext:renderURL": "asset:iap-workflows.WorkflowConsole.js?lazy",
  "defaultOrder": 30
}
